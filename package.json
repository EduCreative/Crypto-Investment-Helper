{
  "name": "crypto-investment-helper",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo \"Object.assign(window.process.env, { API_KEY: '${API_KEY}', CRYPTO_PANIC_API_KEY: '${CRYPTO_PANIC_API_KEY}' });\" > vercel-env.js"
  }
}
